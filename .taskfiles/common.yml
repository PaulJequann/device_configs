version: '3'

vars:
  DOTFILES_REPO: "git@github.com:PaulJequann/dotfiles.git"
  DOTFILES_DIR: "{{.HOME}}/.dotfiles"

tasks:
  setup:
    desc: Common setup tasks for all platforms
    cmds:
      - task: terminal
      - task: dotfiles
      - task: packages
      - echo "✓ Common setup complete!"

  packages:
    desc: Install all packages (Homebrew or platform-native)
    silent: true
    cmds:
      - |
        # Detect Arch and check for Brew override
        USE_BREW_ON_LINUX=${DEVICE_CONFIGS_USE_BREW_ON_LINUX:-0}
        if [ -f /etc/arch-release ] && [ "$USE_BREW_ON_LINUX" -eq 0 ]; then
          task arch:packages
        else
          echo "📦 Installing core CLI tools via Homebrew..."
          brew install git stow zsh neovim tmux fzf ripgrep fd bat eza zoxide go-task gemini-cli
        fi
      - |
        # Skip Homebrew-only taps/packages if on Arch and not using Brew
        USE_BREW_ON_LINUX=${DEVICE_CONFIGS_USE_BREW_ON_LINUX:-0}
        if [ -f /etc/arch-release ] && [ "$USE_BREW_ON_LINUX" -eq 0 ]; then
          echo "📦 Skipping Homebrew-specific taps on Arch..."
        else
          echo "📦 Installing bun via oven-sh/bun tap..."
          brew tap oven-sh/bun 2>/dev/null || true
          if brew list oven-sh/bun/bun &>/dev/null 2>&1; then
            brew upgrade oven-sh/bun/bun 2>/dev/null || true
          else
            brew install oven-sh/bun/bun
          fi
          echo "📦 Installing uv..."
          if brew list uv &>/dev/null 2>&1; then
            brew upgrade uv 2>/dev/null || true
          else
            brew install uv
          fi
          echo "📦 Installing beads tools via steveyegge/beads tap..."
          brew tap steveyegge/beads 2>/dev/null || true
          if brew list bd &>/dev/null 2>&1; then
            brew upgrade bd 2>/dev/null || true
          else
            brew install steveyegge/beads/bd
          fi
          if brew list beads_viewer &>/dev/null 2>&1; then
            brew upgrade beads_viewer 2>/dev/null || true
          else
            brew install beads_viewer
          fi
        fi
      - |
        echo "📦 Installing opencode..."
        if command -v opencode &>/dev/null; then
          echo "  ✓ opencode already installed"
        else
          echo "  Installing opencode via Homebrew..."
          if brew list opencode &>/dev/null 2>&1; then
            brew upgrade opencode 2>/dev/null || true
          else
            brew install opencode
          fi
        fi
      - |
        echo "✓ All packages installed/updated!"

  gui:
    desc: Install GUI applications
    cmds:
      - |
        {{if eq OS "darwin"}}
        echo "📦 Installing macOS GUI apps via Homebrew Cask..."
        brew install --cask claude-code antigravity cursor visual-studio-code zed alacritty discord google-chrome microsoft-edge ollama bruno docker 2>/dev/null || true
        brew install --cask steipete/tap/codexbar 2>/dev/null || true
        {{else}}
        echo ""
        echo "════════════════════════════════════════════════════════════════"
        echo "📋 Windows GUI Applications"
        echo "════════════════════════════════════════════════════════════════"
        echo ""
        echo "Run this command in PowerShell on Windows host:"
        echo ""
        WINGET_JSON="configs/winget-packages.json"
        WINDOWS_PATH=$(wslpath -w "$(pwd)/$WINGET_JSON" 2>/dev/null || echo "")
        if [ -n "$WINDOWS_PATH" ]; then
          echo "   winget import -i '$WINDOWS_PATH' --accept-package-agreements --accept-source-agreements"
        else
          echo "   Navigate to device_configs folder and run: winget import -i configs/winget-packages.json"
        fi
        echo ""
        echo "Includes: Windows Terminal, VS Code, Chrome, Edge, Discord, Docker Desktop,"
        echo "Claude Code, Antigravity, Cursor, Zed, Ollama, Bruno, Alacritty, Steam,"
        echo "GeForce Now, Moonlight"
        echo ""
        {{end}}

  sync:
    desc: Sync device_configs and dotfiles, install new packages
    cmds:
      - |
        echo "🔄 Syncing device_configs..."
        git -C "{{.ROOT_DIR}}" pull --rebase origin main 2>/dev/null || echo "⚠️  Failed to sync device_configs (check git status)"
      - |
        if [ -d "{{.DOTFILES_DIR}}" ]; then
          echo "🔄 Syncing dotfiles..."
          git -C "{{.DOTFILES_DIR}}" pull --rebase origin main 2>/dev/null || echo "⚠️  Failed to sync dotfiles (check git status)"
          echo "🔗 Re-running dotfiles bootstrap..."
          bash "{{.DOTFILES_DIR}}/bootstrap.sh" 2>/dev/null || echo "⚠️  Bootstrap failed"
        else
          echo "📁 Dotfiles not found, run 'task dotfiles' first"
        fi
      - |
        echo ""
        echo "✓ Sync complete! Installing any new packages..."
        task common:packages

  terminal:
    desc: Setup terminal environment (zsh, oh-my-zsh, zgenom)
    cmds:
      - |
        # Install oh-my-zsh if not present
        if [ ! -d "$HOME/.oh-my-zsh" ]; then
          echo "🔧 Installing oh-my-zsh..."
          sh -c "$(curl -fsSL https://raw.githubusercontent.com/ohmyzsh/ohmyzsh/master/tools/install.sh)" "" --unattended > /dev/null 2>&1
        fi
      - |
        # Install zgenom if not present
        if [ ! -d "$HOME/.zgenom" ]; then
          echo "🔧 Installing zgenom..."
          git clone https://github.com/jandamm/zgenom.git "${HOME}/.zgenom" > /dev/null 2>&1
        fi
      - |
        # Prompt to set zsh as default shell
        CURRENT_SHELL=$(basename "$SHELL")
        if [ "$CURRENT_SHELL" != "zsh" ]; then
          echo ""
          printf "🐚 Would you like to set zsh as your default shell? (y/n) "
          read REPLY
          if [ "$REPLY" = "y" ] || [ "$REPLY" = "Y" ]; then
            ZSH_PATH=$(command -v zsh)
            if ! grep -q "$ZSH_PATH" /etc/shells 2>/dev/null; then
              echo "$ZSH_PATH" | sudo tee -a /etc/shells > /dev/null
            fi
            chsh -s "$ZSH_PATH" > /dev/null 2>&1
            echo "✓ Default shell changed to zsh (log out and back in to activate)"
          fi
        fi

  dotfiles:
    desc: Clone and setup dotfiles
    cmds:
      - |
        if [ ! -d "{{.DOTFILES_DIR}}" ]; then
          echo "📁 Cloning dotfiles repository..."
          git clone {{.DOTFILES_REPO}} {{.DOTFILES_DIR}} > /dev/null 2>&1
        else
          cd {{.DOTFILES_DIR}} && git pull > /dev/null 2>&1
        fi
      - |
        if [ -f "{{.DOTFILES_DIR}}/bootstrap.sh" ]; then
          echo "🔗 Bootstrapping dotfiles..."
          bash "{{.DOTFILES_DIR}}/bootstrap.sh"
        else
          # Fallback for backwards compatibility (old dotfiles structure)
          if command -v stow &> /dev/null; then
            echo "🔗 Stowing dotfiles (legacy mode)..."
            cd {{.DOTFILES_DIR}} && stow . 2>/dev/null
          fi
        fi

  ssh:
    desc: Generate SSH key for this device
    cmds:
      - |
        DEVICE_NAME=$(hostname)
        SSH_KEY="$HOME/.ssh/id_ed25519"

        if [ -f "$SSH_KEY" ]; then
          echo "SSH key already exists at $SSH_KEY"
          echo "Public key:"
          cat "$SSH_KEY.pub"
        else
          echo "Generating new SSH key for device: $DEVICE_NAME"
          ssh-keygen -t ed25519 -C "$DEVICE_NAME" -f "$SSH_KEY" -N ""
          echo ""
          echo "SSH key generated! Public key:"
          cat "$SSH_KEY.pub"
          echo ""
          echo "Add this key to your GitHub/GitLab account:"
          echo "  GitHub: https://github.com/settings/keys"
          echo "  GitLab: https://gitlab.com/-/profile/keys"
        fi

  check:
    desc: Check system configuration
    silent: true
    cmds:
      - |
        echo "=== System Check ==="
        echo "OS: {{OS}}/{{ARCH}}"
        echo ""
        echo "=== Homebrew ==="
        if command -v brew &> /dev/null; then
          echo "✓ Homebrew installed ($(brew --version | head -1))"
        else
          echo "✗ Homebrew not installed"
        fi
        echo ""
        echo "=== CLI Tools ==="
        for cmd in git stow zsh neovim tmux fzf rg fd bat eza zoxide bun task gemini; do
          if command -v $cmd &> /dev/null; then
            echo "✓ $cmd"
          else
            echo "✗ $cmd (not found)"
          fi
        done
        echo ""
        echo "=== AI Coding Tools ==="
        for cmd in claude opencode bd; do
          if command -v $cmd &> /dev/null; then
            echo "✓ $cmd"
          else
            echo "○ $cmd (not installed)"
          fi
        done
        echo ""
        echo "=== SSH Key ==="
        if [ -f "$HOME/.ssh/id_ed25519.pub" ]; then
          echo "✓ SSH key exists"
          cat "$HOME/.ssh/id_ed25519.pub"
        else
          echo "✗ No SSH key found (run 'task ssh' to generate)"
        fi
